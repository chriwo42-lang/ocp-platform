apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cluster-config
  namespace: openshift-gitops
  annotations:
    # Wave -1: cluster-config muss vor workloads-app (Wave 0) fertig sein.
    # Grund: argocd-cluster-admin ClusterRoleBinding muss existieren bevor
    # workloads-app versucht Ressourcen in anderen Namespaces anzulegen.
    argocd.argoproj.io/sync-wave: "-1"
spec:
  project: platform
  source:
    repoURL: https://github.com/chriwo42-lang/ocp-platform.git
    targetRevision: main
    path: cluster-config
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: openshift-gitops
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
